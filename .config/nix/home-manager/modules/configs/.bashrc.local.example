# Machine-local bashrc overrides (copy to ~/.bashrc.local and edit)
# This file is sourced at the end of .bashrc
# ~/.bashrc.local is NOT tracked by git — per-machine settings only

# --- SSH key ---
# if [ -z "$SSH_AUTH_SOCK" ]; then
#   eval "$(ssh-agent -s)" > /dev/null
# fi
# ssh-add -l | grep -q id_ed25519 || ssh-add --apple-use-keychain ~/.ssh/id_ed25519 2>/dev/null
